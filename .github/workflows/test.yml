name: Test

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: unicode-backend-icu4j-test-${{ github.ref }}
  cancel-in-progress: true

env:
  ROSWELL_VERSION: "26.02.116"
  CL_REPOSITORY_CLIENT_IMAGE: "ghcr.io/egao1980/cl-repository/cl-repository-client"
  CL_REPOSITORY_CLIENT_VERSION: "latest"
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  JAVA_TOOL_OPTIONS: "--enable-native-access=ALL-UNNAMED --add-opens=java.base/java.lang=ALL-UNNAMED --add-opens=java.base/java.util=ALL-UNNAMED"

jobs:
  test-abcl:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - uses: oras-project/setup-oras@v2
        with:
          version: 1.2.3
      - name: Install Roswell
        shell: bash
        run: |
          curl -sL https://raw.githubusercontent.com/roswell/roswell/master/scripts/install-for-ci.sh | bash
          echo "$HOME/.roswell/bin" >> "$GITHUB_PATH"
      - name: Install ABCL
        shell: bash
        run: |
          ros install abcl-bin
          ros use abcl-bin
      - name: Pull cl-repository-client (OCI)
        shell: bash
        run: |
          echo "${GITHUB_TOKEN}" | oras login ghcr.io -u "${GITHUB_ACTOR}" --password-stdin
          rm -rf /tmp/cl-repo-pull .cl-repository
          mkdir -p /tmp/cl-repo-pull .cl-repository
          CLIENT_VER="${CL_REPOSITORY_CLIENT_VERSION}"
          if [ -z "${CLIENT_VER}" ] || [ "${CLIENT_VER}" = "latest" ]; then
            CLIENT_VER=$(oras manifest fetch "${CL_REPOSITORY_CLIENT_IMAGE}:latest" \
              | python3 -c 'import sys,json; print(json.load(sys.stdin)["annotations"]["org.opencontainers.image.version"])')
            : "${CLIENT_VER:?failed to resolve cl-repository-client from :latest anchor}"
            echo "Resolved cl-repository-client → ${CLIENT_VER}"
          fi
          oras pull "${CL_REPOSITORY_CLIENT_IMAGE}:${CLIENT_VER}" -o /tmp/cl-repo-pull
          for f in /tmp/cl-repo-pull/*.tar.gz; do
            tar -xzf "$f" -C .cl-repository
          done
          CLIENT_DIR="$(find .cl-repository -maxdepth 1 -type d -name 'cl-oci-*' | head -1)"
          : "${CLIENT_DIR:?cl-oci-* not found after oras pull}"
          echo "CL_REPOSITORY_CLIENT_DIR=${CLIENT_DIR}" >> "$GITHUB_ENV"
      - name: Bootstrap client QL deps
        shell: bash
        run: |
          ros -e '(ql:quickload (quote ("yason" "ironclad" "babel" "dexador" "quri" "chipz" "flexi-streams" "cl-ppcre" "cl-base64" "trivial-features" "salza2" "archive" "bordeaux-threads" "rove" "alexandria" "cl-cookie")) :silent t)' -q

      - name: Fetch ICU4J jar
        shell: bash
        run: |
          mkdir -p "$HOME/.cache/cl-stack-icu4j"
          curl -fsSL -o "$HOME/.cache/cl-stack-icu4j/icu4j-78.1.jar" \
            https://repo1.maven.org/maven2/com/ibm/icu/icu4j/78.1/icu4j-78.1.jar
          echo "bbb70d3be23110d7295823eee0c2e896ac3b619b3c0f26168f65eb972df51d2a  $HOME/.cache/cl-stack-icu4j/icu4j-78.1.jar" | sha256sum -c -
          echo "CL_STACK_ICU4J_JAR=$HOME/.cache/cl-stack-icu4j/icu4j-78.1.jar" >> "$GITHUB_ENV"
      - name: cl-repo install deps
        shell: bash
        run: |
          export CL_SOURCE_REGISTRY="$(pwd)//:$(pwd)/${CL_REPOSITORY_CLIENT_DIR}//:"
          ros -l scripts/ci-install.lisp -q
      - name: Test
        shell: bash
        run: |
          export CL_SOURCE_REGISTRY="$(pwd)//:$(pwd)/${CL_REPOSITORY_CLIENT_DIR}//:"
          ros -l scripts/ci-test.lisp -q
